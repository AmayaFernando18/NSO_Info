/**
 * @openapi
 * components:
 *   schemas:
 *     QuickAccess:
 *       type: object
 *       properties:
 *         _id:
 *           type: string
 *         title:
 *           type: string
 *         description:
 *           type: string
 *         url:
 *           type: string
 *         icon:
 *           type: string
 *         order:
 *           type: number
 *         activeStatus:
 *           type: boolean
 *         approved:
 *           type: boolean
 *         approvedBy:
 *           type: string
 *         approvedAt:
 *           type: string
 *           format: date-time
 *         rejected:
 *           type: boolean
 *         rejectedBy:
 *           type: string
 *         rejectionReason:
 *           type: string
 *         rejectedAt:
 *           type: string
 *           format: date-time
 *         createdBy:
 *           type: string
 *         updatedBy:
 *           type: string
 *         isDeleted:
 *           type: boolean
 *         deletedAt:
 *           type: string
 *           format: date-time
 *         deletedBy:
 *           type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 */
